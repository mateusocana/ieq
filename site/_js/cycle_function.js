$(function(){
 	$(".slide ul").cycle({
 		fx:'fade',
 		speed: 3000,
 		timeout: 6500,
 		next: '.next',
 		prev: '.prev',
    });
});

// banner publicidade
$(function(){
 	$(".banner ul").cycle({
 		fx:'cover',
 		speed: 3000,
 		timeout: 6500,
 		next: '.next',
 		prev: '.prev',
    });
});
